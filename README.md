# ConcurrentProgramming

Here you will find the code for our Concurrent Programming course practice (UCM). The code simulates a distributed system for movies where the clients gain
access to them by requesting the server simultaneously. Different parallel programming methods are implemented.
